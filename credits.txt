figma
canva
python, pygame, microsoft, github, git
w3schools
stackoverflow
soundbible.com (guy named Sebastian)
mixkit.co
remove.bg
iconscout.com
vecteezy
cloudconvert.com

# Pop = True
                # PopT = 'A player already thrives on the leaderboard with this name. Kindly enter the new name.'
                # fromsetting = True
                # newName = newuser(changename=True)
                # dataDict = {'name': newName, 'score': score, 'time': time}
                # pushDictData(collection='testcollection',
                #                      data=dataDict)
                # writeBigGame(data['name'], True)

# if forLeadPurpose:
            #     bigGame = bigGameVar()
            #     try:
            #         pushData(Text_Val[:-1], score, time, bigGame)
            #         print('Data sent successfully after changing the name.')
            #     except:
            #         print('Name Changed but data could not be sent due to an unexpected error.')
            #         saveGameDataForLater(Text_Val[:-1], score, t)